# Modules

## Functions

### get_modules(path)
	Example usage:
	```
		var init_files = scan_init_files("res://scripts/")
		print(init_files)  # Outputs: ["init_player.gd", "init_enemy.gd", etc.]
	```
